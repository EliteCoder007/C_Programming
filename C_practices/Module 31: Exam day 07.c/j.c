#include<stdio.h>
#include<string.h>

int main(){
    char str[100];
    scanf("%s", str);
    for(int i = 0; i < strlen(str); i++){
        if(str[0] >= 'A' && str[0] <= 'Z'){
            if(str[i] >= 'a' && str[i] <= 'z'){
                printf
            }
        }
    }
    return 0;
}