// HOMEWORK SET

// a. Write a program to calculate perimeter of rectangle.
// Take sides, a & b, from the user.

/*#include<stdio.h>

int main(){
    int a;
    printf("enter a :  ");
    scanf("%d", &a);
                    //a,b as the sides of rectangle...
    int b;
    printf("enter b : ");
    scanf("%d", &b);

    printf("The Perimeter : %d", 2 * (a + b));
    return 0;


}
*/

// b. Take a number(n) from user & output its cube(n*n*n).
/*
#include<stdio.h>

int main(){
    int n;
    printf("Enter the value of n : ");
    scanf("%d", &n);

    printf("The cube of the n is : %d", n * n * n);
    return 0;
}
*/
// c. Write comments for programs a & b.

//done